apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "db-init.fullname" . }}-test-connection"
  labels:
    {{- include "db-init.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "db-init.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
